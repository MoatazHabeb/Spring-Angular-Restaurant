import { ProductRefreshService } from './product-refresh-service';

describe('ProductRefreshService', () => {
  it('should create an instance', () => {
    expect(new ProductRefreshService()).toBeTruthy();
  });
});
